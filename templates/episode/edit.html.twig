{% extends 'base.html.twig' %}

{% block title %}Edit Episode{% endblock %}

{% block body %}
    <div class="container-fluid ml-4">
        <h1 class="text-center my-4">{{ 'episode.edit'|trans }}</h1>

        {{ include('episode/_form.html.twig', {'button_label': 'update'|trans } ) }}

        <a href="{{ path('episode_index') }}">{{ 'back_list'|trans }}</a>

        {{ include('episode/_delete_form.html.twig') }}
    </div>
{% endblock %}
