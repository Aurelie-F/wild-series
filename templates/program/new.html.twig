{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - New program{% endblock %}

{% block body %}
    <div class="container-fluid ml-4">
        <h1 class="text-center my-4">{{ 'program.add_title'|trans }}</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">{{ 'add'|trans }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}